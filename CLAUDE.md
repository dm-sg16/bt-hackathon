# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This repository is newly initialized and contains no source code yet. This file should be updated once the project is bootstrapped with the actual tech stack, build tooling, and architecture.

## Getting Started

When the project is set up, update this file to include:

- **Build/run commands** — how to install dependencies, start the dev server, and build for production
- **Test commands** — how to run the full test suite and how to run a single test file/case
- **Lint/format commands** — the specific commands used in CI
- **Architecture overview** — how the major modules connect (e.g. frontend → API → DB), not just a file listing
- **Key conventions** — any non-obvious patterns enforced in this codebase (naming, state management, data flow, etc.)
